services:
  central:
    build:
      context: .
      dockerfile: src/app/backend/central/Dockerfile
    container_name: central
    ports:
      - "8000:8000"
    environment:
      - EPSILON=${EPSILON:-8.0}
      - BASE_DIR=/app
    volumes:
      - ./models:/app/models
      - ./data:/app/data:ro
    networks:
      - federated

  hospital_0:
    build:
      context: .
      dockerfile: src/app/backend/hospital/Dockerfile
    container_name: hospital_0
    ports:
      - "8100:8100"
    environment:
      - HOSPITAL_ID=0
      - EPSILON=${EPSILON:-8.0}
      - BASE_DIR=/app
      - HOSPITAL_PASSWORD=${HOSPITAL_0_PASSWORD:-hospital}
      - JWT_SECRET=${JWT_SECRET:-secret}
    volumes:
      - ./models:/app/models
      - ./data:/app/data:ro
      - ./notes:/app/notes
    depends_on:
      - central
    networks:
      - federated

  hospital_1:
    build:
      context: .
      dockerfile: src/app/backend/hospital/Dockerfile
    container_name: hospital_1
    ports:
      - "8101:8101"
    environment:
      - HOSPITAL_ID=1
      - EPSILON=${EPSILON:-8.0}
      - BASE_DIR=/app
      - HOSPITAL_PASSWORD=${HOSPITAL_1_PASSWORD:-hospital}
      - JWT_SECRET=${JWT_SECRET:-secret}
    volumes:
      - ./models:/app/models
      - ./data:/app/data:ro
      - ./notes:/app/notes
    depends_on:
      - central
    networks:
      - federated

  hospital_2:
    build:
      context: .
      dockerfile: src/app/backend/hospital/Dockerfile
    container_name: hospital_2
    ports:
      - "8102:8102"
    environment:
      - HOSPITAL_ID=2
      - EPSILON=${EPSILON:-8.0}
      - BASE_DIR=/app
      - HOSPITAL_PASSWORD=${HOSPITAL_2_PASSWORD:-hospital}
      - JWT_SECRET=${JWT_SECRET:-secret}
    volumes:
      - ./models:/app/models
      - ./data:/app/data:ro
      - ./notes:/app/notes
    depends_on:
      - central
    networks:
      - federated

  hospital_3:
    build:
      context: .
      dockerfile: src/app/backend/hospital/Dockerfile
    container_name: hospital_3
    ports:
      - "8103:8103"
    environment:
      - HOSPITAL_ID=3
      - EPSILON=${EPSILON:-8.0}
      - BASE_DIR=/app
      - HOSPITAL_PASSWORD=${HOSPITAL_3_PASSWORD:-hospital}
      - JWT_SECRET=${JWT_SECRET:-secret}
    volumes:
      - ./models:/app/models
      - ./data:/app/data:ro
      - ./notes:/app/notes
    depends_on:
      - central
    networks:
      - federated

  hospital_4:
    build:
      context: .
      dockerfile: src/app/backend/hospital/Dockerfile
    container_name: hospital_4
    ports:
      - "8104:8104"
    environment:
      - HOSPITAL_ID=4
      - EPSILON=${EPSILON:-8.0}
      - BASE_DIR=/app
      - HOSPITAL_PASSWORD=${HOSPITAL_4_PASSWORD:-hospital}
      - JWT_SECRET=${JWT_SECRET:-secret}
    volumes:
      - ./models:/app/models
      - ./data:/app/data:ro
      - ./notes:/app/notes
    depends_on:
      - central
    networks:
      - federated

  hospital_5:
    build:
      context: .
      dockerfile: src/app/backend/hospital/Dockerfile
    container_name: hospital_5
    ports:
      - "8105:8105"
    environment:
      - HOSPITAL_ID=5
      - EPSILON=${EPSILON:-8.0}
      - BASE_DIR=/app
      - HOSPITAL_PASSWORD=${HOSPITAL_5_PASSWORD:-hospital}
      - JWT_SECRET=${JWT_SECRET:-secret}
    volumes:
      - ./models:/app/models
      - ./data:/app/data:ro
      - ./notes:/app/notes
    depends_on:
      - central
    networks:
      - federated

  hospital_6:
    build:
      context: .
      dockerfile: src/app/backend/hospital/Dockerfile
    container_name: hospital_6
    ports:
      - "8106:8106"
    environment:
      - HOSPITAL_ID=6
      - EPSILON=${EPSILON:-8.0}
      - BASE_DIR=/app
      - HOSPITAL_PASSWORD=${HOSPITAL_6_PASSWORD:-hospital}
      - JWT_SECRET=${JWT_SECRET:-secret}
    volumes:
      - ./models:/app/models
      - ./data:/app/data:ro
      - ./notes:/app/notes
    depends_on:
      - central
    networks:
      - federated

  hospital_7:
    build:
      context: .
      dockerfile: src/app/backend/hospital/Dockerfile
    container_name: hospital_7
    ports:
      - "8107:8107"
    environment:
      - HOSPITAL_ID=7
      - EPSILON=${EPSILON:-8.0}
      - BASE_DIR=/app
      - HOSPITAL_PASSWORD=${HOSPITAL_7_PASSWORD:-hospital}
      - JWT_SECRET=${JWT_SECRET:-secret}
    volumes:
      - ./models:/app/models
      - ./data:/app/data:ro
      - ./notes:/app/notes
    depends_on:
      - central
    networks:
      - federated

  hospital_8:
    build:
      context: .
      dockerfile: src/app/backend/hospital/Dockerfile
    container_name: hospital_8
    ports:
      - "8108:8108"
    environment:
      - HOSPITAL_ID=8
      - EPSILON=${EPSILON:-8.0}
      - BASE_DIR=/app
      - HOSPITAL_PASSWORD=${HOSPITAL_8_PASSWORD:-hospital}
      - JWT_SECRET=${JWT_SECRET:-secret}
    volumes:
      - ./models:/app/models
      - ./data:/app/data:ro
      - ./notes:/app/notes
    depends_on:
      - central
    networks:
      - federated

  hospital_9:
    build:
      context: .
      dockerfile: src/app/backend/hospital/Dockerfile
    container_name: hospital_9
    ports:
      - "8109:8109"
    environment:
      - HOSPITAL_ID=9
      - EPSILON=${EPSILON:-8.0}
      - BASE_DIR=/app
      - HOSPITAL_PASSWORD=${HOSPITAL_9_PASSWORD:-hospital}
      - JWT_SECRET=${JWT_SECRET:-secret}
    volumes:
      - ./models:/app/models
      - ./data:/app/data:ro
      - ./notes:/app/notes
    depends_on:
      - central
    networks:
      - federated

  frontend:
    build:
      context: ./src/app/frontend
      dockerfile: Dockerfile
    container_name: frontend
    ports:
      - "3000:80"
    networks:
      - federated
    depends_on:
      - central

networks:
  federated:
    driver: bridge